#include "main.h"
/**
 * rev_string - a function that reverses a string.
 * @s: string
 */

void rev_string(char *s)
{
int i = 0, c = 0;
char tmp;

while (s[i++])
length++;

for (i = 0; i < c / 2; c++)
{
tmp = s[i];
s[i] = s[c - i - 1];
s[c - i - 1] = t;
}
}
