#!/usr/bin/python3
def is_p(n):
    return str(n) == str(n)[::-1]

def max_p():
    max_p = 0
    for a in range(999, 99, -1):
        for b in range(a, 99, -1):
            prod = a * b
            if is_p(prod) and prod > max_p:
                max_p = prod
    return max_p
print(max_p())
