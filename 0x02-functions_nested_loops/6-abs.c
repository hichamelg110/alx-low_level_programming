#include <stdio.h>
#include <ctype.h>
/**
 * main - a program that prints _putchar
 * followed by a new line.
 * Return: 0.
 */

void main(){
    
for (int i = 'a'; i <= 'z'; i++)

{ 
putchar(i);
}
putchar('\n');
if (islower(c))
return (1);
 
else
return (0);
}
